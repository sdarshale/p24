// For more details, see https://aka.ms/devcontainer.json.
{
  "name": "P24 Pay",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}/",
  "image": "hub.docker.com/nallam4u/javascript-node:latest",
  "postCreateCommand": "npm install --ignore-scripts",
  "postAttachCommand": {
    "Start server": "CI=true npx kill-port --yes 8080 && npm start"
  },
  "forwardPorts": [
    8080
  ],
  "portsAttributes": {
    "8080": {
      "label": "Preview of P24 Pay Flow",
      "onAutoForward": "openBrowser"
    }
  },
  "secrets": {
    "CLIENT_ID": {
      "description": "Sandbox client ID of the application. Uses default credential if none provided.",
      "documentationUrl": "https://developer.paypal.com/dashboard/applications/sandbox"
    },
    "CLIENT_SECRET": {
      "description": "Sandbox secret of the application. Uses default credential if none provided.",
      "documentationUrl": "https://developer.paypal.com/dashboard/applications/sandbox"
    },
    "WEBHOOK_ID": {
      "description": "Sandbox Webhook ID of the application.",
      "documentationUrl": "https://developer.paypal.com/api/rest/webhooks/"
    },
    "PAYPAL_API_BASE": {
      "description": "Sandbox PayPal API base url of the application.",
      "documentationUrl": "https://developer.paypal.com/api/rest/requests/"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "vsls-contrib.codetour"
      ],
      "settings": {
        "git.openRepositoryInParentFolders": "always",
        "files.exclude": {
          "**/.devcontainer": true
        }
      }
    }
  }
}
